# --- Framework y Servidor ---
fastapi==0.128.0
uvicorn[standard]==0.40.0 # Se agrega [standard] para incluir httptools y watchfiles
pydantic[email]==2.12.5 # Se agrega [email] para email-validator
pydantic-settings==2.12.0

# --- Base de Datos y ORM ---
SQLAlchemy==2.0.46
psycopg2-binary==2.9.11 # Adaptador para PostgreSQL
alembic==1.18.3 # Para migraciones de base de datos

# --- Tareas Asíncronas (Celery) ---
celery==5.4.0
redis==5.0.3 # Requerido por Celery como broker y backend

# --- Autenticación y Seguridad ---
passlib[bcrypt]==1.7.4 # Para hashing de contraseñas
python-jose[cryptography]==3.5.0 # Para la creación y validación de JWTs
python-multipart==0.0.22 # Para procesar formularios (ej. subida de archivos)

# --- Dependencias Adicionales y Utilidades ---
python-dotenv==1.2.1 # Para cargar variables de entorno desde .env
anyio==4.12.1 # Dependencia de FastAPI para concurrencia
greenlet==3.3.1
h11==0.16.0
idna==3.11
Mako==1.3.10 # Requerido por Alembic
MarkupSafe==3.0.3
six==1.17.0
typing_extensions==4.15.0
websockets==16.0 # Para la funcionalidad de WebSockets
starlette==0.50.0
colorama==0.4.6 # Requerido por Alembic

# --- Dependencias transitivas (generalmente no se añaden a mano, pero se incluyen para asegurar el build) ---
annotated-types==0.7.0
dnspython==2.8.0
rsa==4.9.1
typing-inspection==0.4.2
